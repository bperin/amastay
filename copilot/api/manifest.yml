# The manifest for the "api" service.
# Read the full specification for the "Load Balanced Web Service" type at:
#  https://aws.github.io/copilot-cli/docs/manifest/lb-web-service/

# Your service name will be used in naming your resources like log groups, ECS services, etc.
name: api
type: Load Balanced Web Service

# Distribute traffic to your service.
http:
  # Requests to this path will be forwarded to your service.
  # To match all requests you can use the "/" path.
  path: '/api'
  # You can specify a custom health check path. The default is "/".
  healthcheck:
    path: '/api/v1/health/check'
  public:
    certificates:
      - arn:aws:acm:us-east-1:422220778159:certificate/d1259492-082d-4ad9-b641-b754caef46b5

# Configuration for your containers and service.
image:
  # Docker build arguments. For additional overrides: https://aws.github.io/copilot-cli/docs/manifest/lb-web-service/#image-build
  build: Dockerfile
  # Port exposed through your container to route traffic to it.
  port: 80

cpu: 256       # Number of CPU units for the task.
memory: 512    # Amount of memory in MiB used by the task.
platform: linux/arm64  # See https://aws.github.io/copilot-cli/docs/manifest/lb-web-service/#platform
count: 1       # Number of tasks that should be running in your service.
exec: true     # Enable running commands in your container.
network:
  connect: true # Enable Service Connect for intra-environment traffic between services.

environments:
  production:
    count: 2   # Using 2 tasks for high availability in production
    cpu: 512   # Double CPU for production workloads
    memory: 1024  # Double memory for production workloads
    http:
      alias:
        - name: 'api.amastay.ai'
          hosted_zone: Z09051732Y0SGEMJ22C94

  staging:
    count: 1
    http:
      alias:
        - name: 'staging-api.amastay.ai'
          hosted_zone: Z09051732Y0SGEMJ22C94
    variables:
      LOG_LEVEL: debug
      API_ENDPOINT: https://api.staging.amastay.ai

  develop:
    count: 1
    http:
      alias:
        - name: 'develop-api.amastay.ai'
          hosted_zone: Z09051732Y0SGEMJ22C94
    variables:
      LOG_LEVEL: debug
      API_ENDPOINT: https://api.develop.amastay.ai